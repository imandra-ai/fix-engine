(jbuild_version 1)

(library
  ( (name        core_printer)
    (public_name core_printer)
    (wrapped  false)
    (libraries (
      core_pp
      core_time_defaults
      core
      yojson
    ))
  )
)
