(rule
  (targets model_tags.ml)
  (deps model_tags.iml)
   (action
    (with-stdout-to model_tags.ml
       (run imandra-extract  model_tags.iml))))
       
(rule
  (targets model_messages.ml)
  (deps model_messages.iml)
   (action
    (with-stdout-to model_messages.ml
       (run imandra-extract  model_messages.iml))))
       
(rule
  (targets model_records.ml)
  (deps model_records.iml)
   (action
    (with-stdout-to model_records.ml
       (run imandra-extract  model_records.iml))))
       
(rule
  (targets state.ml)
  (deps state.iml)
   (action
    (with-stdout-to state.ml
       (run imandra-extract  state.iml))))
       
(rule
  (targets model_app_enums.ml)
  (deps model_app_enums.iml)
   (action
    (with-stdout-to model_app_enums.ml
       (run imandra-extract  model_app_enums.iml))))
       
(rule
  (targets actions.ml)
  (deps actions.iml)
   (action
    (with-stdout-to actions.ml
       (run imandra-extract  actions.iml))))
       
(rule
  (targets model_repeating_groups.ml)
  (deps model_repeating_groups.iml)
   (action
    (with-stdout-to model_repeating_groups.ml
       (run imandra-extract  model_repeating_groups.iml))))
       
(library
  (name venue_types)
  (public_name fix-engine.venue_types)
  (wrapped false)
  (flags (:standard -open Imandra_prelude -warn-error -A+8+39))
  (libraries imandra-base.prelude core core_time_defaults)
)
