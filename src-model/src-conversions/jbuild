(jbuild_version 1)

(library
  ( (name conversions)
    (public_name conversions)
    (wrapped false)
    (flags (:standard -open Imandra_prelude))
    (libraries (core protocol_exts string_factory venue_types))
  )
)
