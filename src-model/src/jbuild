(jbuild_version 1)

(library
  ( (name venue)
    (public_name venue)
    (wrapped false)
    (flags (:standard -open Imandra_prelude))
    (libraries (venue_prelude core core_time_defaults venue_types))
  )
)
