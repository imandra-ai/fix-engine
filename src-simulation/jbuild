(jbuild_version 1)

(library
  ( (name        fix_simulation)
    (public_name fix_simulation)
    (wrapped  false)
    (modules (:standard \ server))
    (libraries 
      ( lwt.unix
        venue
        fix_engine
        conversions
      )
    )
  )
)

(executable
  ( (name server)
    (modules (server))
    (libraries 
      ( lwt.unix
        venue
        fix_engine
        fix_simulation
        protocol_pp
      )
    )
  )
)
