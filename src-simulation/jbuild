(jbuild_version 1)

(library
  ( (name        fix_simulation)
    (public_name fix_simulation)
    (wrapped  false)
    (modules (:standard \ server client))
    (libraries 
      ( lwt.unix
        venue
        fix_engine
        string_factory
        core_pp
      )
    )
  )
)

(executable
  ( (name server)
    (modules (server))
    (libraries 
      ( lwt.unix
        venue
        fix_engine
        fix_simulation
        string_factory
        protocol_pp
      )
    )
  )
)

(executable
  ( (name client)
    (modules (client))
    (libraries 
      ( lwt.unix
        venue
        fix_engine
        fix_simulation
        string_factory
        protocol_pp
        cmdliner
        ZMQ
      )
    )
  )
)
